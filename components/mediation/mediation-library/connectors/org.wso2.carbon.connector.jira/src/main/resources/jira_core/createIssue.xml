<template xmlns="http://ws.apache.org/ns/synapse" name="createIssue">

	<parameter name="jira.issueFields"
		description="a list of required issue fields in JSON format needed to create the issue" />

	<sequence>

	</sequence>
</template>
